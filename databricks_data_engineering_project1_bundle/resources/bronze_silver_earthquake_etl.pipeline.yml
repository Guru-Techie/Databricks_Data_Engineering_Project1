# The main pipeline for databricks_data_engineering_project1_bundle

resources:
  pipelines:
    bronze_silver_earthquake_etl:
     name: Bronze_Silver_PROD
     configuration:
      catalog_name: ${var.catalog}
     libraries:
      - glob:
          include: ../src/DLT_pipelines/Bronze_Silver/transformations/**
     schema: silver
     continuous: false
     development: false
     photon: true
     channel: CURRENT
     catalog: ${var.catalog}
     serverless: true
     root_path: ../src/DLT_pipelines/Bronze_Silver


